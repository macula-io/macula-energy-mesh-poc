defmodule CortexIqDashboardProjectionsTest do
  use ExUnit.Case
  doctest CortexIqDashboardProjections

  test "greets the world" do
    assert CortexIqDashboardProjections.hello() == :world
  end
end
