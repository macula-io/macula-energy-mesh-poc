apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Edge-01: Homes deployment

resources:
  # Namespace definition
  - macula-edge-namespace.yaml

  # WAMP Router (macula-edge namespace)
  - bondy-mesh                       # Partisan mesh node with DNS discovery

  # Edge applications (macula-edge namespace)
  - cortex-iq-homes                  # Connects to local bondy-mesh

  # Infrastructure
  - metallb
  - external-dns

# Namespace Structure:
# - macula-edge: Bondy mesh + Edge applications (homes)
