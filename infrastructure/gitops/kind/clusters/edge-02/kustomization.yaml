apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Edge-02: Utility Providers Only (5 providers, no homes)

resources:
  - metallb
  - external-dns
  - namespace.yaml
  - cortex-iq-utilities              # Connects to hub Bondy
