apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Edge-02: Utility Providers Only (5 providers, no homes)

resources:
  - bondy                             # Bondy edge router with bridge relay to beam-01
  - metallb
  - external-dns
  - namespace.yaml
  - cortex-iq-utilities              # Updated to use local bondy
