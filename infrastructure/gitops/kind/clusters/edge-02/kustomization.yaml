apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Edge-02: Utility Providers Only (5 providers, no homes)

resources:
  # Namespace definition
  - macula-edge-namespace.yaml

  # Edge applications (macula-edge namespace)
  - cortex-iq-utilities              # Connects to hub Bondy

  # Infrastructure
  - metallb
  - external-dns

# Namespace Structure:
# - macula-edge: Edge applications (utilities)
