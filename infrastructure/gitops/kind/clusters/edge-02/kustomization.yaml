apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Edge-02: Belgium Flanders homes (145 homes) + Utility Providers (12 providers)

resources:
  # Namespace definition
  - macula-edge-namespace.yaml

  # Edge applications (macula-edge namespace)
  - bondy-mesh                       # Partisan mesh node with DNS discovery

  # Edge applications (macula-edge namespace)
  - cortex-iq-homes                  # Belgium Flanders homes (145 homes)
  - cortex-iq-utilities              # All Benelux providers (12 providers)

  # Infrastructure
  - metallb
  - external-dns

# Namespace Structure:
# - macula-edge: Edge applications (homes + utilities)
