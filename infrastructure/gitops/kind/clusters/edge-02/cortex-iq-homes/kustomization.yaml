apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Use multiple separate deployments for split home distribution
resources:
  - homes-1.yaml
  - homes-2.yaml
  - ../../../base/secrets/cortex-iq-homes-macula-apikey.yaml
  - ../../../base/bondy-client-config-cm.yaml

namespace: macula-edge

# Edge-02: Belgium Flanders homes split across 2 deployments
# - homes-1: 544 homes (west + central)
# - homes-2: 272 homes (east)
# Total: 816 unique homes
