apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../../../base/cortex-iq-simulation

# Use base namespace (macula-apps) - no override needed
# bondy-credentials secret created at cluster level in macula-apps-secrets.yaml
