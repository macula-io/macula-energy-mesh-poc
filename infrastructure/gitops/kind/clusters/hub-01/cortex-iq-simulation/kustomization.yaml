apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../../../base/cortex-iq-simulation
  - secrets.yaml
  # namespace.yaml removed - using cluster-level namespace-macula-hub.yaml

# Override namespace for hub cluster
namespace: macula-hub
