apiVersion: v1
kind: Service
metadata:
  name: portal
  namespace: macula-hub
  labels:
    app: portal
spec:
  type: NodePort
  selector:
    app: portal
  ports:
  - port: 80
    targetPort: 80
    nodePort: 30003
    protocol: TCP
    name: http
