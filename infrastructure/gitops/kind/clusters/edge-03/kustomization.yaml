apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Edge-03: 50 Flanders homes (persistent UUID7 IDs)

resources:
  - bondy                             # Bondy edge router with bridge relay to beam-01
  - metallb
  - external-dns
  - namespace.yaml
  - cortex-iq-homes                  # Updated to use local bondy
