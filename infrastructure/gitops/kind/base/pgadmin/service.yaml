---
apiVersion: v1
kind: Service
metadata:
  name: pgadmin
  namespace: macula-hub
  labels:
    app: pgadmin
    app.kubernetes.io/name: pgadmin
    app.kubernetes.io/component: database-admin
spec:
  type: ClusterIP
  selector:
    app: pgadmin
  ports:
  - name: http
    protocol: TCP
    port: 80
    targetPort: http
